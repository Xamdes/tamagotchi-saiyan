import { } from './../src/Tamagotchi';
describe('Temp', function()
{
  var temp;

  beforeEach(function() {
    temp = ;
  });

  afterEach(function() {
  });

  it('should show how beforeEach() works', function() {
    console.log(temp);
  });

  it('sample test', function() {
    expect(true).toEqual(true);
  });

});
